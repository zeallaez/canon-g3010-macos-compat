#!/bin/zsh
set -eu

readonly PPD_PATH="/Library/Printers/PPDs/Contents/Resources/CanonIJG3000series.ppd.gz"

if [[ "$(/usr/bin/uname -s)" != "Darwin" ]]; then
  print -u2 -- "Canon G3010 compatibility layer supports macOS only."
  exit 1
fi

if [[ ! -f "${PPD_PATH}" ]]; then
  print -- "Canon G3000 CUPS driver is not installed."
  print -- "Network scanning can still be installed; printing will be skipped."
fi

exit 0
